# 笔记 ⋅ 生成要求（固定规格 v4）

> 适用于 `高等数学笔记` 文件夹下所有章节笔记（数学系列 + 408 系列），后续每章均按此执行。
> v2 更新（2026-09）：新增「学习闭环」必备节、例题双要素（步骤编号 + 验算）、质量审计流程与结构自检项。
> v3 更新：新增文件夹主页 index.html 与「返回主页」导航、全系列交叉复审流程。
> v4 更新（2026-09）：纳入 408 系列（数据结构 / 计算机组成原理 / 操作系统 / 计算机网络）；配图全面 Mermaid 化，新增尺寸可读性硬性规范（见第十部分）。

## 一、形式
1. 单文件 HTML（UTF-8），公式使用 LaTeX，由 MathJax 3 渲染。
2. 配置多 CDN 备用源（jsdelivr → unpkg → bootcdn → npmmirror），保证可联网加载。
3. MathJax 不支持的 LaTeX 命令（如 \oiint）必须在配置中用宏定义解决，禁止出现红色报错乱码。
4. 双击即用，不依赖本地环境；页面含侧边目录、滚动高亮、阅读进度条、回到顶部按钮。
5. HTML 中不得残留 Markdown 语法（`**加粗**`、反引号等），加粗一律用 `<b>`。

## 二、内容
6. 整章全覆盖，不省略知识点（含选学节、附录）。
7. 每个重要定理给出完整证明；确实超纲的（如傅里叶收敛定理）需注明「证明属某课程，此处从略」，不许无说明省略。
8. 每节按：定义 → 方法/步骤模板 → 逐步例题 → 验算 组织。
9. 每个例题必须同时具备：
   - a. ①②③ 步骤编号；
   - b. 验算环节（回代、双算法互验、特殊值、估界、几何/物理意义等至少一种）。
10. 例题中的反例/示例必须在整个定义域上成立（例：Lipschitz 反例要在全区间满足方程），不得使用局部成立的伪例。
11. 数值型验算必须与真值核对（如部分和、近似值、三角函数积分值），不留「约等于」的想当然。
12. 不设「实战练习/自测题」板块；练习以「闭卷自查」形式并入学习闭环（见第三部分）。

## 三、学习闭环（每章必备，置于文件末尾）
13. 每章末尾必须有「能力自查与用法索引」一节，并在目录（TOC）中列出，包含三块：
    - a. **能力清单**：「学完本章你应当能……」，逐条具体、可检验；
    - b. **用法索引表**：「题面特征 → 用什么方法 → 对照哪道例题」三列；
    - c. **闭卷自查**：带 ☐ 的自测任务清单，每条注明「做不出来回哪一小节」。

## 四、页面结构
14. 顶部 Hero：标题 + 副标题 + 标签 chips。
15. 每章至少包含：知识地图、各知识节、解题决策流程、易错点清单、公式速查表、能力自查与用法索引。
16. 框样式规范：
    - 定理 = 主题色框；证明 = 灰底虚线左边框；
    - 例题 = 黄色框；方法/模板 = 绿色框；
    - 定义 = 浅主题色框；提示 = 蓝色框；易错 = 红色框。
17. 响应式（手机单列）+ 打印友好。

## 五、质量审计（生成后必须执行）
18. 结构自检：`<section>` 与 `</section>` 数量平衡、`<div>` 与 `</div>` 平衡、无残留占位标记（如 `<!-- SECxx -->`）、TOC 锚点与节 id 一一对应。
19. 数学复核：所有例题的关键计算独立重算一遍；拿不准的反例/定向/符号优先用第三方视角复核。
20. 引用一致性：文中「如 §4 例2」「引理2」等互引必须真实存在；被删除的内容不得留下悬空引用（如「练习 18」）。
21. 术语与计数一致：「四步法」必须恰好列 4 条；表格列数与内容匹配；同一记号全文含义统一（如矩形区间记号与积分限一致）。

## 六、文件管理
22. 文件统一存放于 `D:\我就认定这个了_再也不删除\高等数学笔记\`。
23. 命名：`<章节名>学习笔记.html`。
24. 只做完整教学版，不另做速查版（除非另行要求）。

## 七、教学口径
25. 面向考研复习，术语与同济版教材一致。
26. 覆盖教材全部内容（含选学节），例题选择典型考法。
27. 「讲透为什么」与「学了怎么用」并重：证明给逻辑，例题给套路，用法索引给入口，自查给反馈。

## 八、笔记库主页与导航（v3 新增）
28. 文件夹根目录必须有 `index.html` 主页：按科目分组（高数 / 线代）的卡片网格，卡片含章节标签、标题、知识点摘要与「打开笔记」链接；样式与笔记同一套视觉语言。
29. 每章笔记的侧边栏顶部必须有「← 返回主页」链接（指向 `index.html`）。
30. 新增、重命名或删除章节笔记时，必须同步更新主页卡片与链接。

## 九、全系列交叉复审（v3 新增）
31. 每轮新增或修改后执行结构自检：TOC 条目数 = 锚点数、`<section>`/`<div>` 开闭平衡、无残留占位标记、四路 CDN 源齐全、「能力自查与用法索引」节存在。
32. 独立数学重算：抽查每份笔记例题的关键步骤，拿不准的（公式符号、定向、含参讨论）优先用第三方视角（子代理）复算；审计报告中的问题须逐条修复并复验。
33. 跨文件口径一致性：同一事实的章节引用全系列统一（如「Cramer 推论补全」指向第三章）；术语、记号、结论口径一致。
34. 修复后重跑第 31 条自检，确保每次改动不破坏结构。

## 十、配图与 Mermaid 规范（v4 新增）
35. 所有结构图、流程图、知识地图、决策流程一律用 Mermaid 渲染，禁止字符画（ASCII art）。
36. 尺寸可读性（手机优先）：Mermaid 一律自然尺寸渲染，禁止缩放到容器宽度；字号 18px。标准配置：
```html
mermaid.initialize({startOnLoad:false,theme:"neutral",securityLevel:"loose",
  themeVariables:{fontSize:"18px"},
  flowchart:{useMaxWidth:false,htmlLabels:true,nodeSpacing:30,rankSpacing:40},
  sequence:{useMaxWidth:false}});
```
37. 超宽图允许横向滑动，并提供滑动提示：样式 `pre.mermaid{overflow-x:auto;-webkit-overflow-scrolling:touch}`、`pre.mermaid svg{max-width:none !important}`；渲染完成后自动检测（scrollWidth > clientWidth 时）在图下方追加 `.mermaid-hint` 提示「↔ 图较宽，可左右滑动查看完整内容」。
38. 画图主动控宽：知识地图分支多时用 `<br/>` 把子项合并进同一节点；决策流程避免同一行铺过多并列叶子。
39. Mermaid 标签内禁止裸 `<`（与 HTML 同规则）；用「小于 / ≤」或 `&lt;` 表达；标签内可用 `<br/>` 换行。
40. 配图渲染后必须实测（线上打开或 DOM 校验），确保无语法错误、尺寸可读。
41. 代码示例一律放 `pre.code`（深色代码框）；代码注释内禁用 ①-⑩、∅、⌊⌋⌈⌉ 等怪符，序号统一用 `(1)(2)(3)`。
42. 标准 Mermaid 加载器（三 CDN 备用；onload 内嵌第 36 条配置 + 第 37 条滑动提示逻辑）：
```html
<script>
(function(){
  var murls=[
    "https://cdn.jsdelivr.net/npm/mermaid@10.9.1/dist/mermaid.min.js",
    "https://unpkg.com/mermaid@10.9.1/dist/mermaid.min.js",
    "https://registry.npmmirror.com/mermaid/10.9.1/files/dist/mermaid.min.js"
  ];
  function mload(i){
    if(i>=murls.length){return;}
    var s=document.createElement("script");
    s.src=murls[i];s.async=true;
    s.onload=function(){try{mermaid.initialize({/* 见第 36 条 */});
      mermaid.run({querySelector:".mermaid"}).then(function(){
        document.querySelectorAll("pre.mermaid").forEach(function(p){
          if(p.scrollWidth>p.clientWidth+8){
            var d=document.createElement("div");d.className="mermaid-hint";
            d.textContent="↔ 图较宽，可左右滑动查看完整内容";
            p.insertAdjacentElement("afterend",d);
          }});});}catch(e){}};
    s.onerror=function(){mload(i+1);};
    document.head.appendChild(s);
  }
  mload(0);
})();
</script>
```
